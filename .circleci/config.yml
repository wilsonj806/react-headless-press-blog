version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  test-react:
    executor:
      name: node/default
    steps:
      - checkout
      - run: ls
      - run: cd 'react-app'
      # - run: npm ci
      # - run: npm run build
workflows:
    test:
      jobs:
        - test-react
